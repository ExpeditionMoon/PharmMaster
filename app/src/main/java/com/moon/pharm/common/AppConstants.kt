package com.moon.pharm.common

object AppConstants {
    // Coil
    const val DIR_IMAGE_CACHE = "image_cache"
    const val CACHE_MEMORY_PERCENT = 0.15
    const val CACHE_SIZE_BYTES = 50L * 1024 * 1024
}